import { Renderable } from "../Renderable";
import { pageNameKey, StaticPageData } from "../../OptionalPlugins/StaticPages/StaticPageManager";
export declare class RenderableStaticPage extends Renderable {
    readonly [pageNameKey]: string;
    readonly title: string;
    readonly date: Date;
    readonly url: string;
    readonly body: string;
    readonly __head: string;
    readonly meta: Array<{
        [attr: string]: string;
    }>;
    constructor(data: StaticPageData);
    templateCandidates(): string[];
}
