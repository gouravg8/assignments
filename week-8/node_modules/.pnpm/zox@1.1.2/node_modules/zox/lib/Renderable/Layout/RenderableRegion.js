"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const Renderable_1 = require("../Renderable");
class RenderableRegion extends Renderable_1.Renderable {
    get content() {
        let content = '';
        for (let i = 0; i < this.blocks.length; ++i) {
            content += this.blocks[i].toString();
        }
        return content;
    }
    constructor(region, blocks) {
        super('region');
        this.region = region;
        this.blocks = blocks ? blocks : [];
    }
    templateCandidates() {
        const candidates = super.templateCandidates();
        candidates.push('region-' + this.region);
        return candidates;
    }
}
exports.RenderableRegion = RenderableRegion;
//# sourceMappingURL=RenderableRegion.js.map