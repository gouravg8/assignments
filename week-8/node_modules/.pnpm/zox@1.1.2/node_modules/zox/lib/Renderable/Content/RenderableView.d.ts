import { $metadata, IRenderable, Renderable, RenderingMetadata } from "../Renderable";
export declare type Link = {
    title: string;
    url: string;
};
export declare type PagerOptions = {
    currentPageNumber?: number;
    first?: Link;
    prev?: Link;
    current?: Link;
    next?: Link;
    last?: Link;
};
export declare class RenderableView extends Renderable {
    items: Array<IRenderable>;
    private readonly mode;
    private readonly pager?;
    constructor(mode?: string, items?: Array<IRenderable>, pager?: PagerOptions);
    templateCandidates(): string[];
    readonly [$metadata]: RenderingMetadata;
}
