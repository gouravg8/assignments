import { IRenderable, Renderable } from "../Renderable";
export declare class RenderableBlock extends Renderable {
    block?: string;
    constructor(block?: string, content?: IRenderable, settings?: any);
    templateCandidates(): string[];
}
export declare abstract class ConditionallyRenderableBlock extends RenderableBlock {
    toString(): string;
    abstract show(): boolean;
}
