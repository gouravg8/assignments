import { $metadata, IRenderable, RenderingMetadata } from "../Renderable";
export declare class PureRenderableList implements IRenderable {
    type?: string;
    title?: string;
    [$metadata]?: RenderingMetadata;
    items: Array<IRenderable>;
    constructor(items?: Array<IRenderable>);
    toString(): string;
}
