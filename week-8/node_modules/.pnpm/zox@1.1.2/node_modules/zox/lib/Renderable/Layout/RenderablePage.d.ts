import { Renderable } from "../Renderable";
import { RenderableRegion } from "./RenderableRegion";
export declare type PageRegions = {
    [region: string]: RenderableRegion;
};
export declare class RenderablePage extends Renderable {
    constructor(regions: PageRegions);
}
