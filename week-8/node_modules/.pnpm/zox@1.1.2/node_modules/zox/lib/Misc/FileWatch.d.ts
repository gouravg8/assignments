/// <reference types="node" />
import * as fs from "fs";
export declare type FileInfo = {
    filePath: string;
    stats: fs.Stats;
};
export declare type FileEvent = {
    event: 'changed' | 'added' | 'removed' | 'initial';
    info: FileInfo;
};
export declare type FileEventHandler = (e: FileEvent) => void;
export declare function watch(directory: string, handler: FileEventHandler, ms: number): NodeJS.Timer;
export declare function listFileStatsAsync(directory: string): Promise<Array<FileInfo>>;
