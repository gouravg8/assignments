import { RenderableBlock } from "../../Renderable/Layout/RenderableBlock";
import { IRenderable } from "../../Renderable/Renderable";
export declare class BlockContent extends RenderableBlock {
    content: IRenderable;
    constructor(block: string, content: IRenderable, settings?: any);
    templateCandidates(): string[];
}
export declare class BlockPureContent implements IRenderable {
    type?: string;
    content: IRenderable;
    constructor(block: string, content: IRenderable);
    toString(): string;
}
