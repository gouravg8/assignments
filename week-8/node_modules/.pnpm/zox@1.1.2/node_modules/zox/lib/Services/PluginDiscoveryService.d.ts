import { IPluginDiscovery, PluginDefinition } from "zox-plugins";
import { IService } from "../ServiceContainer";
export declare const serviceKey: unique symbol;
export declare abstract class IPluginDiscoveryService implements IService, IPluginDiscovery {
    readonly serviceKey: symbol;
    abstract getPlugins(pluginName: symbol): Array<PluginDefinition>;
    abstract add(pluginKey: symbol, plugin: any, data: any): void;
    abstract scan(obj: any): void;
    abstract scanModule(moduleExports: any): void;
    abstract scanDirectory(directory: string): Promise<void>;
    abstract scanProject(directory: string): Promise<void>;
    abstract scanNodeModules(directory: string): Promise<void>;
    abstract clear(): void;
}
