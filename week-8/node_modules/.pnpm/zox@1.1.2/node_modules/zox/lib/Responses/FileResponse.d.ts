/// <reference types="node" />
import { IResponse } from "./IResponse";
import { OutgoingHttpHeaders, ServerResponse } from "http";
import * as fs from "fs";
export declare type FileWithStats = {
    filePath: string;
    stats: fs.Stats;
};
export declare class FileResponse implements IResponse {
    statusCode: number;
    headers: OutgoingHttpHeaders;
    private readonly file;
    private readonly inline;
    private readonly setContentDisposition;
    constructor(file: string | FileWithStats, inline?: boolean, statusCode?: number, headers?: OutgoingHttpHeaders, setContentDisposition?: boolean);
    send(response: ServerResponse): void;
    private sendFile;
}
