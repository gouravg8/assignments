/// <reference types="node" />
import { IResponse } from "./IResponse";
import { OutgoingHttpHeaders, ServerResponse } from "http";
export declare class EventStreamResponse implements IResponse {
    statusCode: number;
    headers: OutgoingHttpHeaders;
    onClose: () => void;
    private response;
    private headWritten;
    private dataQueue;
    readonly connected: boolean;
    constructor(statusCode?: number, headers?: OutgoingHttpHeaders, onClose?: () => void);
    send(response: ServerResponse): void;
    sendMessage(data: any, event?: string): void;
    close(): void;
}
